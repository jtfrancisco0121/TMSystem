# Inventory + Invoice (Electron)

Run locally:

1. Install dependencies

```bash
npm install
```

2. Start the app

```bash
npm start
```

Notes:
- This scaffold uses React + Babel via CDN for simplicity (development/demo). Replace with a bundler for production.
